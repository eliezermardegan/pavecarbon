document.addEventListener('DOMContentLoaded',()=>{const f=document.getElementById('leadForm');if(!f)return;f.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(f));const subject=encodeURIComponent('PaveCarbon project enquiry — '+data.company);const body=encodeURIComponent(Object.entries(data).map(([k,v])=>`${k}: ${v}`).join('\n'));const msg=document.getElementById('formMsg');msg.textContent='Your enquiry is prepared. If the PaveCarbon email channel is active, use the generated email action below.';const a=document.createElement('a');a.href=`mailto:info@pavecarbon.com?subject=${subject}&body=${body}`;a.textContent='Open email draft →';a.className='text-link';msg.after(a);});});
